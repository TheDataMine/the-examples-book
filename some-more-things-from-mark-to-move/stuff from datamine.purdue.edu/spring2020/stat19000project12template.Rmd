---
title: "Project 12 Solutions"
output:
  pdf_document: default
  html_document:
    df_print: paged
---

## Student Name: ...

## Peer Collaborators (if any) and nature of the collaboration ...
## TA help sought (if any) ...
## Online resources used (if any) and weblink for location of resources ...

### 1a *(1 pt)*
Did you use the MySQL database? Why or why not? ...

```{r eval=F}
r code here, create other code chunks, like below, if you choose not to use MySQL
```

```{bash eval=F}
```


### 1b *(1 pt)*
Did you use the MySQL database? Why or why not? ...

```{r eval=F}
r code here, create other code chunks, like below, if you choose not to use MySQL
```

```{bash eval=F}
```

### 1c *(1 pt)*

```{r eval=F}
r code here, create other code chunks, like below, if you choose not to use MySQL
```
<!-- To include your image in RMarkdown, you can uncomment
the following line, move your image to the same folder as your RMarkdown file, and replace "./image_name.jpg" with "./your_image_name.jpg" where "your_image_name.jpg" is the full name of the image you want to include. -->

<!-- ![resulting plot](./image_name.jpg) -->


### 2a *(2 pts)*
```{r eval=F, include=T}
my_string <- "CREATE TABLE fake_table (
  variable1 TEXT,
  my_key INTEGER PRIMARY KEY,
  variable2 TEXT
)"
```

### 2b *(2 pts)*
```{r eval=F, include=T}
my_string <- "CREATE TABLE fake_table (
  variable1 TEXT,
  my_key INTEGER PRIMARY KEY,
  variable2 TEXT
)"
```

### 2c *(1.5 pts)*
```{r eval=T}
# r code here
```

### 2d *(1.5 pts)*
```{r eval=T}
# r code here
```
